package com.test;

import java.io.File;
import java.io.IOException;
import java.util.List;

import org.apache.avro.file.DataFileWriter;
import org.apache.avro.io.DatumWriter;
import org.apache.avro.specific.SpecificDatumWriter;

import com.bdcor.bio.data.avro.dict;
import com.bdcor.bio.data.service.DictDeSeriralizeService;
//import com.jet.bio.data.avro.User;

public class RWTest {

	public static void main(String[] args) throws IOException {

		/*User user1 = new User();
        user1.setName("zhangsan");
   String path = "D:\\user.avro"; // avro文件存放目录
   DatumWriter<User> userDatumWriter = new SpecificDatumWriter<User>(User.class);
   DataFileWriter<User> dataFileWriter = new DataFileWriter<User>(userDatumWriter);
   dataFileWriter.create(user1.getSchema(),  new File(path));
// 把生成的user对象写入到avro文件
   
   dataFileWriter.close();*/
	}

}
